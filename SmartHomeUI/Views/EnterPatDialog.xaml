<Window x:Class="SmartHomeUI.Views.EnterPatDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SmartThings PAT"
        Height="200"
        Width="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Style="{StaticResource GlassPanel}" Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Add meg a SmartThings PAT-et" Style="{StaticResource NeonTitleStyle}" Margin="0,0,0,8"/>
            <PasswordBox x:Name="PatBox" Grid.Row="1" Style="{StaticResource GlassPasswordBox}" />
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="OK" Width="100" Style="{StaticResource GlassActionButton}" Margin="0,0,8,0" Click="Ok_Click"/>
                <Button Content="Mégse" Width="100" Style="{StaticResource GlassActionButton}" Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
