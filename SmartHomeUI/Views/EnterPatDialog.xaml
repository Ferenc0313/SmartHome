<Window x:Class="SmartHomeUI.Views.EnterPatDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SmartThings PAT"
        Height="240"
        Width="520"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource DialogWindowStyle}">
    <Border Style="{StaticResource GlassPanel}"
            Padding="18"
            Margin="8"
            Background="{StaticResource DialogBackdrop}"
            BorderThickness="2"
            Effect="{StaticResource DialogGlow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{StaticResource DialogTitleBarBrush}"
                    CornerRadius="10,10,0,0"
                    Padding="10,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <DockPanel LastChildFill="False">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="pack://application:,,,/SmartHomeUI;component/Assets/Icons/Main_Icon.png"
                               Width="18"
                               Height="18"
                               Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                                   VerticalAlignment="Center"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                    <Button DockPanel.Dock="Right"
                            Width="32"
                            Height="24"
                            Padding="0"
                            Margin="8,0,0,0"
                            Style="{StaticResource GlassActionButton}"
                            Click="Cancel_Click">
                        <TextBlock Text="X"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"/>
                    </Button>
                </DockPanel>
            </Border>

            <Border Grid.Row="1"
                    Padding="14"
                    CornerRadius="10"
                    Background="{StaticResource DialogHeaderBrush}"
                    BorderBrush="{StaticResource NeonCyan}"
                    BorderThickness="0,0,0,2">
                <DockPanel>
                    <Border Width="10"
                            Height="36"
                            CornerRadius="6"
                            Background="{StaticResource NeonCyan}"
                            VerticalAlignment="Center"
                            Opacity="0.7"
                            Margin="0,0,12,0"/>
                    <StackPanel>
                        <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}"
                                   Style="{StaticResource NeonTitleStyle}"
                                   FontSize="26"/>
                        <TextBlock Text="Add meg a SmartThings PAT-et"
                                   Foreground="{StaticResource TextDimmed}"
                                   FontSize="12"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <StackPanel Grid.Row="2" Margin="0,12,0,12">
                <PasswordBox x:Name="PatBox" Style="{StaticResource GlassPasswordBox}" />
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="OK" Width="100" Style="{StaticResource GlassActionButton}" Margin="0,0,8,0" Click="Ok_Click"/>
                <Button Content="MÆcgse" Width="100" Style="{StaticResource GlassActionButton}" Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
